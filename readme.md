# Basic temporary usage instructions
```
# Run the following command in the root directory of the project (Must have docker compose installed)
$ docker compose up

# Run the following in a new terminal then check the output in the docker terminal
$ curl http://localhost:8000/topics/<email@email.com>
```
